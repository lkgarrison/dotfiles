#!/bin/bash

# install git-completion (tab-completion of branch names, etc)
eval curl -s https://raw.githubusercontent.com/git/git/master/contrib/completion/git-completion.bash -o ~/.git-completion.bash
